import React from 'react'
import UserTemplate from '../../templates/UserTemplate'

const UserOrdersContainer = (props) => {
  return (
    <UserTemplate {...props}>
      <div>
        Welcome to user orders container
      </div>
    </UserTemplate>
  )
}

export default UserOrdersContainer